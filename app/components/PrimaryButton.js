'use client';
import { Button } from 'keep-react';

export const PrimaryButton = () => {
  return (
    <Button className="rounded-none primary_button" variant="outline">
      Button
    </Button>
  );
};
